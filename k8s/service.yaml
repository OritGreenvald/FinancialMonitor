apiVersion: v1
kind: Service
metadata:
  name: financialmonitor-api
spec:
  selector:
    app: financialmonitor-api

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080

  type: LoadBalancer
